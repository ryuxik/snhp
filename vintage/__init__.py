"""VINTAGE — the make-an-offer store sim (one-of-one inventory, LES 2026).

Three arms over the identical sourcing + browser stream:
  sticker/1  — tag price, take it or leave it; the cultural −20%-at-30-days
               gut markdown (the control).
  offer/1    — make-an-offer: browsers submit shaded offers; the engine
               accepts/counters per item against an event-consistent
               disagreement value (waiting for a future connecting browser,
               hazard learned censoring-aware from its own sales history).
  hazard/1   — ablation: the same learned per-item hazard drives COMPUTED
               markdowns, no offers (H-V3).
"""
