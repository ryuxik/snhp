"""BOBA — the vend sim grown into a made-to-order shop (vend/BOBA.md, P0).

The scarce resource is barista-minutes, not shelf stock: a FIFO queue,
wait-sensitive balking, tapioca batches on a 4-hour clock, and pickup-time
as a bundle issue. Pure math — no LLM calls anywhere in this package.
"""
