"""Irreconcilable Agents — the divorce demo's engine-side package.

Build order (divorce/SPEC.md §9): the headless kill harness comes FIRST —
personas + ARM-I/ARM-O over oracle utilities, 100 seeds, zero LLM, zero UI.
Chrome is built only if K1–K3 survive (SPEC.md §8).
"""
