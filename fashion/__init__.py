"""FASHION P0 — one buy, one season, no restock: markdown-vs-cliff sim.

Pure math, no LLM calls anywhere. Self-contained (does not import vend/,
which is being edited concurrently); shared primitives are copied into
fashion/core.py with the same semantics.
"""
